# Wonder 后端功能测试文档

## 测试环境

- **服务地址**: http://127.0.0.1:9800
- **启动命令**: `npm run dev` 或 `npm run dev:electron`
- **测试工具**: 浏览器开发者工具、Postman、curl

---

## 1. 基础服务测试

### 1.1 服务启动

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 服务正常启动 | 运行 `npm run dev` | 控制台显示 "Wonder server running on http://127.0.0.1:9800" |
| 健康检查 | 访问 `GET http://127.0.0.1:9800/api/health` | 返回 `{"status":"ok"}` |

### 1.2 静态文件服务

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 前端页面加载 | 浏览器访问 `http://127.0.0.1:9800` | 显示前端页面 |
| SPA 路由回退 | 访问任意前端路由（如 `/settings`） | 显示前端页面（非 404） |

---

## 2. 配置管理测试

### 2.1 获取配置

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 获取所有配置 | `GET /api/config` | 返回 JSON 对象，包含所有配置项 |

### 2.2 更新配置

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 设置 LLM API Key | `PUT /api/config` body: `{"llm_api_key":"sk-xxx"}` | 返回 `{"success":true}` |
| 设置 LLM Base URL | `PUT /api/config` body: `{"llm_base_url":"https://api.openai.com/v1"}` | 返回 `{"success":true}` |
| 设置 LLM Model | `PUT /api/config` body: `{"llm_model":"gpt-4o-mini"}` | 返回 `{"success":true}` |
| 配置持久化 | 设置配置后重启服务，再次获取配置 | 配置值保持不变 |

---

## 3. 文献分析测试

### 3.1 单篇文献分析

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 提交分析请求 | `POST /api/analysis/single` body: `{"fileName":"test.pdf","fileType":"pdf","text":"文献内容..."}` | 返回 SSE 流 |
| SSE 事件流 | 观察 SSE 事件 | 依次收到 `step` 事件（literature, relation, writing, todo, matching） |
| 分析完成 | 等待 SSE 流结束 | 收到 `complete` 事件，包含 `documentId` |
| 步骤状态 | 观察每个 step 事件 | 每个步骤先收到 `running` 状态，再收到 `done` 状态 |

### 3.2 前端集成测试

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 文件上传 | 在前端上传 PDF/TXT 文件 | 文件内容被提取并显示 |
| 分析进度 | 提交分析后观察进度条 | 进度条随步骤推进 |
| 分析结果 | 分析完成后查看结果 | 显示文献解析、关联分析、写作素材、待办提取、匹配评分 |

---

## 4. 知识库管理测试

### 4.1 文档列表

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 获取文档列表 | `GET /api/knowledge` | 返回文档数组，按创建时间倒序 |
| 空列表 | 清空数据库后获取列表 | 返回空数组 `[]` |

### 4.2 文档删除

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 删除文档 | `DELETE /api/knowledge/:id` | 返回 `{"success":true}` |
| 级联删除 | 删除文档后查询其 chunks | chunks 已被删除 |
| 删除不存在的文档 | `DELETE /api/knowledge/nonexistent` | 返回 `{"success":true}`（不报错） |

---

## 5. 历史记录测试

### 5.1 历史列表

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 获取历史列表 | `GET /api/history` | 返回历史记录数组，按创建时间倒序 |
| 限制数量 | `GET /api/history?limit=10` | 最多返回 10 条记录 |
| 默认限制 | `GET /api/history` | 默认返回 50 条记录 |

### 5.2 历史详情

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 获取历史详情 | `GET /api/history/:id` | 返回历史记录详情 |
| 不存在的记录 | `GET /api/history/nonexistent` | 返回 404 `{"error":"Not found"}` |

---

## 6. LLM 服务测试

### 6.1 API 调用

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 未配置 API Key | 清空 `llm_api_key` 配置后调用 | 抛出错误 "LLM API key not configured" |
| 正常调用 | 配置正确的 API Key 后调用 | 返回 LLM 响应内容 |
| 流式调用 | 使用 `callStream` 方法 | 逐块返回响应内容 |

### 6.2 错误处理

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 无效 API Key | 使用错误的 API Key | 抛出 LLM API 错误 |
| 网络错误 | 断开网络后调用 | 抛出网络错误 |
| 无效模型 | 配置不存在的模型名 | 抛出模型错误 |

---

## 7. 数据库测试

### 7.1 文档操作

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 插入文档 | 调用 `upsertDocument` | 文档成功插入 |
| 更新文档 | 使用相同 ID 再次调用 `upsertDocument` | 文档被更新（UPSERT） |
| 查询文档 | 调用 `getDocument` | 返回正确的文档数据 |
| 列表文档 | 调用 `listDocuments` | 返回所有文档 |
| 删除文档 | 调用 `deleteDocument` | 文档被删除 |

### 7.2 分块操作

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 插入分块 | 调用 `insertChunk` | 分块成功插入 |
| 查询分块 | 调用 `getChunksByDocument` | 返回文档的所有分块 |
| 删除分块 | 调用 `deleteChunksByDocument` | 文档的所有分块被删除 |

### 7.3 配置操作

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 设置配置 | 调用 `setConfig` | 配置成功设置 |
| 获取配置 | 调用 `getConfig` | 返回正确的配置值 |
| 更新配置 | 使用相同 key 再次调用 `setConfig` | 配置被更新 |
| 获取所有配置 | 调用 `getAllConfig` | 返回所有配置键值对 |

### 7.4 历史操作

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 添加历史 | 调用 `addHistory` | 历史记录成功添加 |
| 查询历史 | 调用 `getHistory` | 返回正确的历史记录 |
| 列表历史 | 调用 `listHistory` | 返回历史记录列表 |

---

## 8. 工具函数测试

### 8.1 文本分块

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 短文本 | `chunkText("hello world")` | 返回单个分块 |
| 长文本 | `chunkText("a".repeat(15000), 7000, 500)` | 返回多个分块，每个 ≤7000 字符 |
| 空文本 | `chunkText("")` | 返回空数组 |
| 参数验证 | `chunkText("test", -1)` | 抛出错误 "maxChars must be positive" |

### 8.2 Token 估算

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 基本估算 | `estimateTokens("hello world test")` | 返回 `Math.floor(16 / 1.5)` |

### 8.3 向量操作

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 相同向量相似度 | `cosineSimilarity([1,0,0], [1,0,0])` | 返回 1.0 |
| 正交向量相似度 | `cosineSimilarity([1,0,0], [0,1,0])` | 返回 0.0 |
| 相反向量相似度 | `cosineSimilarity([1,0], [-1,0])` | 返回 -1.0 |
| 向量搜索 | `searchVectors(query, candidates, 2)` | 返回最相似的 2 个向量 |

---

## 9. 错误处理测试

### 9.1 参数验证

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 缺少必填字段 | POST 请求缺少必要字段 | 返回 400 错误 |
| 无效 JSON | POST 请求 body 不是有效 JSON | 返回 400 错误 |

### 9.2 数据库约束

| 测试点 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 外键约束 | 插入不存在的 document_id 的 chunk | 抛出外键约束错误 |
| 主键冲突 | 插入重复 ID 的文档 | 执行 UPSERT 更新 |

---

## 10. 完整流程测试

### 10.1 文献分析完整流程

1. **配置 LLM**
   - 设置 `llm_api_key`
   - 设置 `llm_base_url`
   - 设置 `llm_model`

2. **上传文献**
   - 选择 PDF/TXT 文件
   - 提交分析请求

3. **等待分析**
   - 观察进度条
   - 等待所有步骤完成

4. **查看结果**
   - 查看文献解析
   - 查看关联分析
   - 查看写作素材
   - 查看待办提取
   - 查看匹配评分

5. **管理知识库**
   - 查看文档列表
   - 删除文档

6. **查看历史**
   - 查看历史列表
   - 查看历史详情

---

## 测试记录表

| 测试日期 | 测试模块 | 测试点 | 结果 | 备注 |
|----------|----------|--------|------|------|
| | | | | |

---

## 已知问题

（在此记录测试过程中发现的问题）

| 问题描述 | 严重程度 | 状态 |
|----------|----------|------|
| | | |
